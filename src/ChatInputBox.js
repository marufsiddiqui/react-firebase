import React from 'react';

export const ChanelInputBox = () => {
  return (
    <form className="ChatInputBox">
      <input className="ChatInput" placeholder="Message #general" />
    </form>
  );
}
